version: '3.9'
services:
  backend:
    build: ./backend
    env_file:
      - backend/.env
    ports:
      - "3978:3978"
  frontend:
    image: nginx:alpine
    volumes:
      - ./frontend:/usr/share/nginx/html:ro
    ports:
      - "8080:80"
    depends_on:
      - backend
